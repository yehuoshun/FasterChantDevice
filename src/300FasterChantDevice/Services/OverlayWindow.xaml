<Window x:Class="_300FasterChantDevice.Services.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Width="300" Height="400"
        Cursor="None">

    <Border Background="#CC1a1a2e"
            BorderBrush="#FFe94560"
            BorderThickness="1"
            CornerRadius="8"
            Padding="12">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBlock Grid.Row="0"
                       Text="咏唱装置"
                       Foreground="#FFe94560"
                       FontSize="14" FontWeight="Bold"
                       Margin="0,0,0,8"/>

            <!-- Content area -->
            <ItemsControl Grid.Row="1"
                          x:Name="ContentList"
                          Foreground="White"
                          FontSize="16">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Margin="0,4"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Border>
</Window>
