# FasterChantDevice — 设计文档

> 300 高速咏唱装置 2.0  
> 基于 FasterChantDevice (Anran-233) 理念重写  
> 技术栈：C# / .NET 8 / WPF / Windows only

---

## 一、交互模型

### 主动模式（快捷发言）
```
F1 → 主面板（10 个命名分组，1-9, 0）
       │
       └→ 按数字键 → 二级发言
              ├── 连发 ON  → 文本框逐行依次发出
              └── 连发 OFF → 随机选一行发出
```

### 被动模式（条件触发）
```
OCR 双引擎检测游戏事件（K/D/A 计数 + 击杀播报）
  ├── 开局 → 自动发送
  ├── 击杀 → 自动发送
  ├── 死亡 → 自动发送
  ├── 助攻 → 自动发送
  └── 骚话 → 手动 F2 / 定时 N 秒 / 两者均可
```

### 骚话冷却
击杀/死亡/助攻触发后，默认 5 秒内不触发骚话，避免上下文尴尬。

---

## 二、OCR 事件检测

### 定位方式
- 零校准：按游戏窗口比例自动计算检测区域
- `FindWindow` 获取窗口 → 比例映射坐标
- 用户可选微调（百分比滑块）

### 三引擎
| 引擎 | 目标 | 频率 | 可靠性 |
|------|------|------|--------|
| K/D/A 数字 OCR | 右上角计数区 | 500ms | 95%+ |
| 击杀播报 OCR | 中上弹出区 | K/D 变化后 500ms 窗口 | 受皮肤影响 |
| 像素变化 | 播报区域 | 持续 | 100%，零字体依赖 |

### 融合逻辑
```
K/D/A 变化 →
  ├── 击杀+1 → OCR 播报区 → 确认击杀 → 发击杀台词
  │              └ OCR 失败 → 仍判定为击杀（数字不骗人）
  ├── 死亡+1 → 同上
  └── 助攻+1 → 同上
```

---

## 三、数据结构

### 英雄方案 (heroes/{hero_name}.json)
```json
{
  "name": "卫宫",
  "triggers": {
    "game_start": ["来了来了", "这把稳了"],
    "kill": ["不过如此", "下一个"],
    "death": ["我的我的", "..."],
    "assist": ["好配合"],
    "taunt": {
      "boxes": [
        ["你搁这刮痧呢？", "就这？"],
        ["吾之生涯一片无悔", "这便是我的全部了"]
      ]
    }
  },
  "panels": [
    { "name": "集合", "lines": ["来这里", "集合打团", "别分散", "跟上"] },
    { "name": "撤退", "lines": ["撤！", "别打了快跑"] }
  ]
}
```

### 全局设置 (settings.json)
```json
{
  "trigger_key": "F1",
  "taunt_key": "F2",
  "burst_mode": true,
  "burst_interval_ms": 1000,
  "taunt_mode": "both",
  "taunt_interval_s": 30,
  "taunt_cooldown_s": 5,
  "game_window_class": "300Heroes",
  "ocr_kda_region": { "x_ratio": 0.80, "y_ratio": 0.02, "w_ratio": 0.18, "h_ratio": 0.10 },
  "ocr_broadcast_region": { "x_ratio": 0.25, "y_ratio": 0.05, "w_ratio": 0.50, "h_ratio": 0.12 }
}
```

---

## 四、进程模型

```
单进程多线程
├── UI 线程（WPF）
├── 键盘钩子线程（WH_KEYBOARD_LL，回调 <1ms）
├── OCR 检测线程（游戏中激活，3fps）
├── 看门狗线程（500ms 检查钩子存活）
└── 热键备胎（RegisterHotKey 兜底）
```

---

## 五、打包分发

- GitHub Actions 自动构建
- 产物：便携 ZIP + 单文件 EXE
- 自包含发布（.NET 8 运行时内置）
- 自动更新：GitHub Releases API 检查 → 下载替换

---

## 六、UI 页面

| 页面 | 说明 |
|------|------|
| 英雄编辑 | 条件触发 Tab + 快捷发言 Tab（左列表右编辑） |
| 设置 | 连发、触发键、OCR 微调、骚话配置、自动更新 |
| 游戏覆盖面板 | F1 呼出的半透明穿透面板 |
| 系统托盘 | 最小化到托盘，右键菜单 |
